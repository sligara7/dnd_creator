[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
log_cli = true
log_cli_level = INFO
markers =
    integration: marks tests as integration tests (select with '-m integration')
    load: marks tests as load tests (select with '-m load')
    unit: marks tests as unit tests (select with '-m unit')